# pnpm 11 reads build-script approvals from here, not from package.json.
# These packages ship native binaries that must compile for Tailwind, Next,
# and the ESLint resolver to work.
allowBuilds:
  "@tailwindcss/oxide": true
  sharp: true
  unrs-resolver: true
